'use client'

// Adsterra removed - now using Monetag
// This component is kept empty to avoid breaking imports
// Will be replaced with MonetagBanner once codes are provided

export function AdsterraBanner({ slotKey, className }: { slotKey: string; className?: string }) {
  return null
}
